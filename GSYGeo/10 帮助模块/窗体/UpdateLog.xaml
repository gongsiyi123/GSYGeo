<Window x:Class="GSYGeo.UpdateLog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GSYGeo"
        mc:Ignorable="d"
        Title="更新日志" Height="450" Width="500" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer Grid.Row="0" Name="UpdateLogScrollViewer" HorizontalAlignment="Center">
            <TextBox Name="UpdateLogTextBox" IsReadOnly="True" Margin="5" TextBlock.LineHeight="18" Background="#FFEAEAEA" TextWrapping="Wrap"/>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="确定" Margin="5" Click="Button_Click" IsDefault="True" IsCancel="True"/>
        </Grid>
    </Grid>
</Window>
